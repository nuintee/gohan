import { ResultsEntity } from '@/hooks/context/Restaurants/types'
import { RestaurantProps } from '@/types/Restaurant'

const Restaurant = (props: RestaurantProps<ResultsEntity>) => {
  return <div>rest</div>
}

export default Restaurant
